name: CI (Build & Test)

on:
  push:
    branches: "**"
  pull_request:
    branches: [ "main" ]
    types: [ opened, synchronize, reopened, ready_for_review ]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write
  id-token: write

jobs:
  build:
    name: CI (Build & Test)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle (cache)
        uses: gradle/actions/setup-gradle@v4


    # - name: Build, test & coverage
    #   run: TODO1


    # - name: Publish unit test results
    #   uses: TODO2
    #   if: ${{ TODO3 && (TODO4 || TODO5) }}
    #   with:
    #     files: TODO6

#   dependency-submission:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: TODO7
#       if: ${{ TODO8 }}
#       steps:
#         - uses: TODO9
#
#         - name: Set up JDK 17
#           uses: TODO10
#           with:
#             distribution: TODO11
#             java-version: TODO12
#
#         - name: Generate and submit dependency graph
#           uses: TODO13
#           continue-on-error: TODO14



      - name: TODO – build, tests, coverage
        run: echo "Build, run tests, upload a test report, and build the app artifact. (You can delete this after successfully implementing the TODOS)"



